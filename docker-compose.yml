version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pablos-carpinteria
    ports:
      - "8080:80"
    restart: unless-stopped

# Para correr:
#   docker-compose up --build
#
# Luego abrir: http://localhost:8080